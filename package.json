{
  "name": "cypress-farhad",
  "version": "1.0.0",
  "description": "for training only",
  "main": "index.js",
  "scripts": {
    "cypress:open": "npx cypress open",
    "cypress:run": "rimraf cypress/results/ && cypress run && npx mochawesome-merge 'cypress/results/*.json' > mochawesome.json && npx marge mochawesome.json && rimraf mochawesome.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/farhadfabregas/cypress.git"
  },
  "keywords": [
    "cypress"
  ],
  "author": "farhad",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.6.6",
    "cypress-cucumber-preprocessor": "^4.3.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "rimraf": "^5.0.5"
  }
}
